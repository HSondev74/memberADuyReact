const Header = (props) => {
     return (
          // <header onClick={() => props.setWork(!props.work)}>
          //      Header {props.work && "Aba Duy"}
          // </header>
          <header >Click</header>
     );
};

export default Header;
